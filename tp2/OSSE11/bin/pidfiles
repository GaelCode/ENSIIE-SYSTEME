#!/bin/sh

if [ $# -ne 1 ]; then
    echo "Usage: $0 <directory>"
    exit 1
elif [ ! -d "$1" ]; then
    echo "The argument needs to be a directory"
    exit 1
fi

find "$1" -type f -exec stat -c "%U" {} \; | uniq -c

# full flemme